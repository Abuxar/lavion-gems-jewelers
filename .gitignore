node_modules/
server/node_modules/
.env
server/.env
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

.vercel
.env*

# The template carries no secrets and must stay in version control.
!.env.example

# Next.js build output and generated types — never commit it
.next/
next-env.d.ts

# TypeScript's incremental build cache, written by any `tsc` run
tsconfig.tsbuildinfo
